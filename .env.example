# Copy to .env.local for development. Set by StoreForge on Vercel in production.
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
SHOPIFY_STOREFRONT_TOKEN=
# Optional — template must never require it:
SHOPIFY_ADMIN_TOKEN=
# Canonical/OG URLs:
NEXT_PUBLIC_SITE_URL=http://localhost:3000
