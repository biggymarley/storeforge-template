import type { SeoConfig } from "@/lib/types/config";

export const seoConfig: SeoConfig = {
  defaultTitle: "Placeholder Store",
  titleTemplate: "%s | Placeholder Store",
  description: "Quality goods, delivered. Shop the Placeholder Store catalog.",
  ogImage: "/branding/og-image.png",
  twitterHandle: ""
};
